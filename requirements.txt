# requirements.txt — TPM AI Phase 0 baseline
# ref: MASTER_PLAN_v6.md § 17 (OSS catalog) + § 22.2
# updated: 2026-05-09 — G-01 (llama-index), G-06 (version bounds), G-09 (mem0→chromadb)

# ============================================================
# Core orchestration
# ============================================================
langgraph>=0.2.0
langchain-core>=0.3.0
pydantic>=2.5.0

# ============================================================
# Local LLM client
# ============================================================
ollama>=0.4.0
litellm>=1.50.0       # OpenAI-compatible proxy + router

# ============================================================
# Knowledge — 3 layers
# ============================================================
chromadb>=0.5.0,<0.6       # Layer 2 vector DB (+ user_memory collection แทน Mem0 — G-09)
# Layer 1 — markitdown + llama-index (ทดแทน openkb+pageindex — G-01)
markitdown>=0.0.1          # Microsoft MIT — PDF/Word/Excel → Markdown
llama-index>=0.11,<0.12    # document ingestion + query engine
llama-index-embeddings-ollama>=0.3,<0.4
llama-index-vector-stores-chroma>=0.2,<0.3
# pageindex — NOT on PyPI (removed per G-01)
# openkb    — NOT on PyPI (removed per G-01)

# ============================================================
# Search (Layer 3) — lane assignment per § 6.4.0
# ============================================================
# Tavily — AI-optimized output for LLM context (1,000/mo free)
tavily-python>=0.5.0
# Exa.ai — structured + grounding for cited engineering reports (1,000/mo free)
exa-py>=1.0
# DuckDuckGo Instant Answer — simple lookup, no API key (unlimited)
# NOTE: package was renamed from duckduckgo-search to ddgs (mid-2025)
ddgs>=9.0
# Wikipedia REST — reference / definitions, no API key (unlimited)
wikipedia-api>=0.7
# Note: SearXNG runs as Docker service (workhorse) — no Python package needed
# Note: Brave Search dropped — EOL Feb 2026
# Page fetch
crawl4ai>=0.3.0       # OSS headless scraping
trafilatura>=1.12     # main content extraction
beautifulsoup4>=4.12
httpx>=0.27           # async HTTP (also used for Jina Reader r.jina.ai/<url>)

# ============================================================
# Browser automation
# ============================================================
playwright>=1.48      # ERP web automation

# ============================================================
# Document generation (OSS-first)
# ============================================================
python-docx>=1.1
python-pptx>=1.0
openpyxl>=3.1
xlsxwriter>=3.2
reportlab>=4.2
weasyprint>=63        # backup PDF generator
pypdf>=5.0
camelot-py[base]>=0.11
# markitdown — already declared in Knowledge section (G-01)

# ============================================================
# Vision / image
# ============================================================
opencv-python>=4.10
pillow>=11.0
imagehash>=4.3
magika>=0.6           # Google file-type detection
# ultralytics — AGPL, install separately if needed
# torchvision — alternative to ultralytics (BSD)

# ============================================================
# Engineering math (deterministic — replaces AI calc)
# ============================================================
sympy>=1.13
numpy>=2.0
scipy>=1.14
pandas>=2.2
polars>=1.10
pint>=0.24            # unit-aware calculations

# ============================================================
# System monitoring
# ============================================================
psutil>=6.0
GPUtil>=1.4
nvidia-ml-py>=12      # accurate VRAM
watchdog>=5.0
xxhash>=3.5

# ============================================================
# Memory
# ============================================================
# mem0ai — แทนด้วย ChromaDB collection "user_memory" (G-09: ไม่ต้องการ service แยก)
# mem0ai>=0.1.40  ← commented out

# ============================================================
# Observability (self-hosted)
# ============================================================
langfuse>=2.50
arize-phoenix>=5.0

# ============================================================
# Code quality / DevSecOps
# ============================================================
ruff>=0.7
bandit>=1.7
mypy>=1.13
pytest>=8.3
pytest-asyncio>=0.24
detect-secrets>=1.5
pip-audit>=2.7
coverage>=7.6

# ============================================================
# UI
# ============================================================
chainlit>=1.3
markdown-it-py>=3.0

# ============================================================
# Misc
# ============================================================
python-dotenv>=1.0
pyyaml>=6.0
rich>=13.9            # CLI pretty print
typer>=0.13           # CLI builder
